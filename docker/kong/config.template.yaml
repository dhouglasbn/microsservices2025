_format_version: "3.0"
_transform: true

services:
  - name: orders
    url: ${ORDERS_SERVICE_URL} # Nosso localhost pelo config do docker
    port: 3333
    path: / 
    routes:
      - name: api-orders
        protocols:
          - http
          - https
        paths:
          - '/orders' # apigateway.com/orders faz uma requisição na api de orders

  - name: invoices
    url: ${INVOICES_SERVICE_URL} # Nosso localhost pelo config do docker
    port: 3334
    path: / 
    routes:
      - name: api-invoices
        protocols:
          - http
          - https
        paths:
          - '/invoices' # apigateway.com/invoices faz uma requisição na api de invoices

plugins:
  - name: cors
    config:
      origins:
        - "*"
      methods:
        - GET
        - POST
        - PUT
        - DELETE
        - OPTIONS
      headers:
        - Accept
        - Content-Type
        - Authorization